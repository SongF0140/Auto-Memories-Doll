# 面向 AI 的架构验收与技术分析文档

## 1. 文档用途

这份文档用于让 AI 读取项目架构图后，判断项目是否已经完成，以及完成度到什么水平。

它不是普通说明书，而是一份**验收型文档**。AI 需要按模块逐项检查：

- 架构边界是否成立
- 数据流是否闭环
- 记忆系统是否真的可持续
- 快处链路和主链路是否都能工作
- 提示词、标签、索引、审计、回写是否联动
- 每一层是否有可验证证据，而不是只有概念

## 2. 先给结论

从图上看，这个项目不是单纯的“聊天应用”，而是一个带有以下特征的 AI 操作框架：

1. **双路径响应**
   - 快处路径负责低延迟即时回复
   - 主链路负责复杂编排、索引、审计和回写

2. **统一消息入口**
   - 来自窗口、浏览器书签、MCP、skills、后台 JSON 的信息要被统一处理

3. **多层记忆体系**
   - 短期记忆
   - 长期记忆
   - 个性化标签
   - 向量索引
   - 图谱信息

4. **持续自更新闭环**
   - 输出不是终点
   - 输出会进入审计、对比、压缩、更新、本地回写

5. **面向 AI 的可检查性**
   - 系统最终目标不是“看起来能用”
   - 而是“能被 AI 判断哪些模块完成了，哪些没完成”

## 3. 架构总览

### 3.1 前端层

前端基于 React 框架。

前端的职责不是做重推理，而是做**控制面**：

- 人机交互
- 索引查询匹配
- 纠错入口
- 记忆模式开关
- 提示词开关
- 本地数据更新触发

从架构角度看，前端更像一个“操作台”，它应该把用户意图转换成标准输入，而不是自己承担复杂业务逻辑。

### 3.2 后台编排层

后台是系统的**数据面 + 决策面**。

它负责把多源输入统一成可处理消息，再完成：

- 接入
- 清洗
- 拆包
- 过滤
- 分类
- 评分
- 索引
- 输出
- 审计
- 回写

如果前端是驾驶舱，后台就是发动机和变速箱。

### 3.3 本地记忆层

本地记忆系统存放在本地磁盘（图中标注为 C 盘、D 盘）。

它至少包含三类内容：

- 结构化记忆 JSON
- 向量索引
- 图谱信息

它的任务是让系统从“单轮问答”升级为“可持续积累的个性化系统”。如果没有这一层，整个项目只能算一个带 UI 的推理入口，不算完整架构。

## 4. 关键技术分析

### 4.1 为什么要分“快处路径”和“主链路”

这是整个图最重要的设计点之一。

#### 快处路径的价值

快处路径解决的是**响应速度**问题：

- 本地读取个性化标签
- 拼接用户输入
- 直接进入 mini LLM
- 走本地索引
- 返回即时结果

这类路径的目标不是最强推理，而是：

- 低延迟
- 低依赖
- 少中间状态
- 快速反馈

它适合简单检索、局部纠错、即时回答、模板化响应。

#### 主链路的价值

主链路解决的是**复杂性管理**问题。

它适合：

- 多源输入融合
- JSON 结构化消息
- 分包拆解
- 噪声过滤
- 分类路由
- 图/向量索引
- 审计
- 压缩
- 更新本地记忆

这条链路比快处路径重，但换来的是长期可维护性和更强的上下文组织能力。

#### 结论

这不是重复设计，而是典型的“**快路径负责体验，慢路径负责正确性和积累**”。

### 4.2 为什么需要统一 JSON 入口

图里出现了很多输入源：

- 后台监听窗口传入 JSON
- 浏览器书签抓取 JSON
- MCP + skills 读取 JSON

这说明系统不是单一来源输入，而是一个多源聚合系统。

统一 JSON 的意义在于：

1. **降低耦合**
   - 不同输入源的原始格式不一致
   - 统一 JSON 后，后续模块只面对一种协议

2. **利于分包和路由**
   - 结构化消息更容易拆分、打标签、评分和路由

3. **利于审计**
   - 任何一次系统决策都能追溯到标准输入

4. **利于回放**
   - JSON 可持久化、可重放、可对比

技术上，这意味着后台需要一个消息契约层：

- source
- timestamp
- user input
- context
- tags
- confidence
- route
- memory_write_flag
- audit_trace

如果没有这层契约，后面的索引和记忆都会变得不可控。

### 4.3 预处理为什么要放在中间层

图里的预处理包括：

- 监听整合处理
- MCP + skills 拆分 JSON
- 去噪过滤（模糊 LLM）

这个设计的核心是：**不要让原始输入直接进入索引和记忆**。

原因是：

- 原始消息往往有重复、噪声、断句、缺字段
- 直接索引会污染向量库和图谱
- 直接写入记忆会把噪声永久化

所以预处理层本质上是一个“数据净化器”：

1. 合并碎片
2. 去掉无效片段
3. 标准化字段
4. 补齐上下文
5. 再进入分类和索引

这一步如果做得不好，后面所有模块都会被污染。

### 4.4 分类索引的作用

图中有两类与分类相关的内容：

- JSON 格式化 + 分类注入
- 置信度评分

这意味着系统并不是只做检索，而是做**分类驱动的路由系统**。

分类索引的价值有三层：

1. **选择策略**
   - 不同消息进入不同处理分支

2. **提升召回质量**
   - 相似消息能被聚合，而不是散落在索引里

3. **支持后续记忆写入**
   - 类别可以影响是否进入长时记忆、是否压缩、是否回写标签

置信度评分尤其重要，因为它决定：

- 这个消息是高可信事实
- 还是待确认推测
- 还是低置信噪声

如果没有置信度，系统会把“猜测”和“事实”混在一起，记忆会越来越脏。

### 4.5 索引构建为什么是三种并存

图里同时出现：

- 图索引
- Embedding 向量索引
- 分类索引

这不是重复，而是分工不同。

#### 图索引

适合表达实体、关系、依赖、演化顺序。

例如：
- 用户 -> 输入 -> 结果
- 任务 -> 子任务 -> 审计 -> 更新
- 标签 -> 提示词 -> 输出

#### 向量索引

适合语义相似召回。

适用于：
- 找相近历史案例
- 找相近提示词
- 找相近上下文

#### 分类索引

适合做粗路由和规则过滤。

适用于：
- 快速判断该走哪条链路
- 判断是否进入记忆
- 判断是否需要审计

#### 结论

三者一起用，说明你的系统想要的是：

- 结构关系
- 语义相似
- 业务分类

这是一种更稳的混合检索架构。

### 4.6 输出扉页的意义

输出扉页不是“展示页”这么简单，它是一个**阶段性结果汇聚点**。

它应该承担：

- 当前任务结果摘要
- 关键中间结论
- 审计引用
- 回写建议
- 后续更新触发信号

技术上，扉页是前台与后台之间的稳定接口。

如果没有扉页，前端就只能直接对接复杂编排结果，系统会变得难维护。

### 4.7 审计与差异对比为什么重要

图中提到：

- markdown 流式转码 + LLM
- 新旧改差异对比
- fork / append 的冲突分支

这说明你不只想要生成，还想要**可审查、可比较、可追责**。

这是一个很强的工程信号。

#### markdown 流式转码 + LLM

这说明系统要把过程转成可读文本，让 AI 能检查：

- 有没有遗漏
- 有没有结构错误
- 有没有版本差异

#### 新旧对比

这是典型的版本控制思路：

- 新输出和旧输出是否一致
- 改动是否可解释
- 是否引入了回归

#### fork / append 冲突分支

这代表系统会面对多来源、多版本、多分支内容。

如果没有冲突治理：

- 记忆会分叉
- 标签会漂移
- 提示词会被互相覆盖

所以审计不是附属功能，而是记忆系统能否长期工作的保险丝。

### 4.8 本地自动更新 loop 的工程意义

图里最后一层是本地自动更新 loop：

- 本地文件系统索引更新和管理
- 更新本地 loop 修改个性化标签
- 写回本地个性化前缀提示词和标签

这说明系统不是静态的，而是动态自我修正的。

它的实现逻辑应当类似：

1. 从输出和审计中提炼新增知识
2. 判断是否值得写入
3. 压缩成更短的记忆单元
4. 更新标签、前缀词、向量、图谱
5. 下一轮再参与决策

这条 loop 是整个项目最接近“智能体”的部分。

## 5. 模块级验收要点

### 5.1 前端 UI

验收问题：
- 是否真的是 React 结构
- 是否存在明确的输入、开关、提示词、纠错交互
- 是否有记忆模式切换
- 是否能把状态传给后台

### 5.2 快处模块

验收问题：
- 是否能读取本地个性化标签
- 是否能拼接用户输入
- 是否能走 mini LLM
- 是否具备即时响应和流式输出

### 5.3 后台采集

验收问题：
- 是否支持窗口 JSON
- 是否支持浏览器书签抓取
- 是否支持 MCP / skills 输入
- 是否统一成一个标准消息结构

### 5.4 预处理模块

验收问题：
- 是否有拆包
- 是否有去噪
- 是否有整合
- 是否有字段标准化

### 5.5 分类与评分模块

验收问题：
- 是否有分类注入
- 是否有置信度评分
- 是否能支撑路由分支

### 5.6 索引模块

验收问题：
- 是否有图索引
- 是否有向量索引
- 是否有分类索引
- 是否支持相似度检索

### 5.7 输出扉页

验收问题：
- 是否能汇总关键结果
- 是否是可供审计和回写的稳定接口

### 5.8 审计与版本对比

验收问题：
- 是否能生成可读审计文本
- 是否能做新旧版本差异分析
- 是否能识别分支冲突

### 5.9 本地自动更新 loop

验收问题：
- 是否会更新本地文件系统索引
- 是否会更新个性化提示词和标签
- 是否会把新知识回写到记忆系统

## 6. 风险分析

### 6.1 过度复杂风险

这个架构很强，但也很容易复杂化。

风险点：
- 输入源太多
- 记忆层太多
- 索引层太多
- 分支治理过于复杂

如果没有统一数据契约，系统会很快失控。

### 6.2 记忆污染风险

如果预处理、去噪、置信度评分做不好：

- 错误会被写进长期记忆
- 低质量标签会反复影响后续决策
- 索引会越来越脏

### 6.3 回写震荡风险

自动更新 loop 如果太激进，会导致：

- 提示词频繁变化
- 标签漂移
- 输出不稳定

所以回写必须有阈值、置信区间和审计门槛。

### 6.4 单点责任不清风险

如果前端、后台、记忆层职责混在一起：

- 调试困难
- 测试困难
- 回滚困难

所以模块边界必须清晰。

## 7. AI 该如何判断“完成”

AI 不应该只看“有没有文件”，而应看以下证据链是否成立。

### 7.1 结构完成

- 前端、后台、记忆层是否存在
- 是否有清晰接口
- 是否可独立理解和替换

### 7.2 数据完成

- 输入是否能进入系统
- 中间结果是否能处理
- 输出是否能回写

### 7.3 闭环完成

- 输出是否会影响下一轮
- 标签是否会更新
- 记忆是否会积累

### 7.4 稳定性完成

- 是否有去噪
- 是否有置信度评分
- 是否有审计
- 是否有差异对比
- 是否有冲突治理

## 8. 建议的 AI 检查输出格式

建议 AI 最终按这个格式回报：

```text
1. 前端 React UI
   结论：已完成 / 部分完成 / 未完成
   证据：
   风险：

2. 快处路径
   结论：已完成 / 部分完成 / 未完成
   证据：
   风险：

3. 后台主链路
   结论：已完成 / 部分完成 / 未完成
   证据：
   风险：

4. 记忆闭环
   结论：已完成 / 部分完成 / 未完成
   证据：
   风险：

5. 审计与更新 loop
   结论：已完成 / 部分完成 / 未完成
   证据：
   风险：
```

如果某项是“部分完成”，AI 应明确写出：

- 缺了哪一步
- 会影响哪条链路
- 最小补齐方案是什么

## 9. 最终判断

这张图表达的不是一个普通 AI 项目，而是一个**带记忆、带审计、带回写、带双路径路由的本地 AI 协作系统**。

真正完成的标志不是某个页面能跑，而是：

1. 用户输入能进来
2. 系统能分流
3. 结果能索引
4. 证据能审计
5. 结论能回写
6. 下一轮行为真的会被上一次结果影响

如果这六步都成立，这个项目才算在架构意义上完成。
