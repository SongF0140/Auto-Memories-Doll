我来先检索这些原项目，确认准确信息后再为你整理完整笔记。
以下是结合原内容和检索补充后的完整笔记：

---

![AI Agent 记忆系统五范式对比](sandbox:///mnt/agents/output/agent_memory_framework.png)

---

# AI Agent 记忆系统：五大范式笔记

## 一、总览：五个项目的定位

| 项目 | 核心定位 | 层级 | 关键思想 |
|------|---------|------|---------|
| **Text to Mem** | 记忆操作语言 | 基础设施层 | 用 IR（中间表示）统一所有记忆操作 |
| **Mem0** | 记忆中间件 | 中间件层 | 即插即用的生产级记忆层 |
| **Letta** | 记忆操作系统 | 系统层 | OS 虚拟内存思想 + Agent 自编辑 |
| **Rainy** | 文件级透明记忆 | 用户层 | 记忆控制权还给用户 |
| **memU** | 主动记忆 Agent | Agent 层 | **记忆本身是一个 Agent** |

> **核心演进线**：被动存储 → 主动管理 → 自治进化 → 预测先行 → **记忆即 Agent**

---

## 二、各项目详解

### 1. Text to Mem — 记忆操作语言

**定位**：不是做一个记忆系统，而是为**所有记忆系统定义通用操作语言**。

**核心设计**：

| 维度 | 内容 |
|------|------|
| **IR 层** | 在 LLM 和存储之间插入中间表示层，类比 CPU 指令集架构 |
| **12 原子操作** | 分三阶段：**ENC**（encode，记忆诞生）→ **RET**（retrieve/summarize，取回/摘要）→ **STO**（9 个操作，覆盖记忆全生命周期）|
| **五元 JSON** | `{阶段, 操作名, 目标, 参数, 元数据}` |
| **retrieve vs summarize 分离** | 工程关键：retrieve 几十毫秒，summarize 是完整推理（延迟上千倍），分离后可用不同超时/缓存/降级策略 |
| **安全校验** | 外层：JSON Schema 格式校验；里层：业务逻辑校验（如 `promote` 操作的绝对值和相对值必须恰好一个为空）|
| **硬性安全规则** | 危险操作（如"删除所有记忆"）必须二选一：`dry-run` 模拟 或 `confirmation: true` |
| **Lock 四种模式** | `read-only`（冻结）/ `delete` / `append-only` / `custom`（可编程），支持 RBAC 风格的 review 绕过机制 |

**评价**：目前存储只有 SQLite 参考实现，无 HTTP API，向量与 JSON 文本混存。但其真正价值是帮行业定义"记忆操作语言应该长什么样"。

---

### 2. Mem0 — 当下最流行的记忆中间件

**定位**：给任何 Agent 框架"即插即用"的记忆层。

**项目背景**：
- GitHub **~48K–65K stars**，开源社区热度最高的记忆框架之一
- **Y Combinator 孵化**，融资 **$24M**
- 支持 Python + JavaScript SDK

**三层架构**：

```
┌─────────────────┐
│   Memory API    │  ← 开发者调用的接口层
├─────────────────┤
│ 推理/向量/检索   │  ← 五个工厂模式（核心）
├─────────────────┤
│    存储层       │  ← 向量库 + 图库
└─────────────────┘
```

**五大工厂模式**（体现工程成熟度）：

| 工厂 | 支持数量 | 说明 |
|------|---------|------|
| LLM Factory | 17+ 提供商 | 动态加载（`importlib`），未安装包不挂 |
| Embedding Factory | 11+ 模型 | |
| Vector Store Factory | 22+ 向量存储 | |
| Graph Store Factory | 4 种图存储 | |
| Reranker Factory | 5 种重排序器 | |

**三种记忆类型**：

| 类型 | 定义 | 特点 |
|------|------|------|
| **语义记忆** | 抽象的事实性知识 | 通用知识、用户偏好 |
| **情景记忆** | 具体事件 | 某次对话、某个场景 |
| **程序记忆** | Agent 执行的完整步骤 | **逐字保留**，用于崩溃后恢复执行状态 |

**关键工程设计**：

1. **UUID 幻觉处理**：LLM 对长 UUID 处理能力差，会幻觉。Mem0 将 UUID 临时映射为 `0,1,2...` 简单整数，Agent 决定"对第 3 条记忆执行操作"，系统再映射回真实 UUID。

2. **双存储并行**：向量存储负责语义相似搜索，图存储负责关系推理。两条路径用 `ThreadPoolExecutor` 并行跑，结果合并返回。

3. **双 Prompt 策略**：
   - `user_memory_extraction_prompt`：只看用户消息
   - `agent_memory_extraction_prompt`：只看助手消息
   - **价值**：防止助手自我表达污染用户记忆，同时允许 AI 积累"我是一个什么样的 AI"的自我认知。

4. **多层级作用域隔离**：用户隔离 → Agent 隔离 → 运行时隔离，直接支持多用户、多 Agent、会话隔离。

**性能数据**（官方声称）：
- 相比直接用 OpenAI Memory：准确率高 **26%**，响应快 **91%**，Token 省 **90%**

**成本瓶颈**：
- 每次调用完整模式会调用 **2–5 次 LLM**
- Token 数随记忆规模**线性增长**
- **不适合高频实时写入**，适合中低频场景（对话助手、客服、医疗健康追踪）

**核心机制**：两阶段管道 — **Extraction（提取）→ Update（更新）**。系统自动判断是 `ADD` 新事实、`UPDATE` 已有事实，还是 `IGNORE` 冗余。

---

### 3. Letta — 记忆自治最彻底的"操作系统"

**定位**：把**操作系统虚拟内存思想**完整搬进 Agent 架构。

**项目背景**：
- 原名 **MemGPT**，UC Berkeley Sky Computing Lab 2023 年 10 月论文
- 2024 年 9 月正式更名为 **Letta**
- GitHub **~21K stars**，融资 **$10M**（Felicis）

**三层记忆架构**（类比 OS）：

| 层级 | 类比 | 实现 | 特点 |
|------|------|------|------|
| **Core Memory** | RAM | 建在 system prompt 里，每次推理都可见 | 三元组结构：`label`（路径/命名空间）+ `description` + `value` + `limit`（默认 10 万字符，强制信息压缩）|
| **Archival Memory** | 磁盘 | 向量检索 | 容量无限，长期存储 |
| **Recall Memory** | 日志系统 | 存放所有历史对话消息 | 完整对话历史 |

**核心机制**：

1. **信息蒸馏约束**：Core Memory 的 `limit` 默认 10 万字符，逼迫 Agent 主动做信息蒸馏。

2. **上下文驱逐**：当上下文窗口快满时，触发 `summarize`，默认驱逐 30% 消息到 Recall Memory。被驱逐的消息**不是消失**，只是从 in-context 移到 out-of-context —— 这才是真正的"虚拟内存无损分层"。

3. **Git-enabled Block Manager**：
   - Git 是真实来源，PG 数据库是快速读缓存
   - 每次变更产生一次 Git commit（携带 Agent ID、时间戳、变更原因）
   - **好处**：不可变性、内容寻址、防止静默损坏、完整历史回溯、并发安全（多 Agent 用 Git tree 隔离）、可审计

4. **MemGPT FS**：记忆组织成真正的目录树：
   - `/persona`：Agent 人格设定
   - `/human`：用户偏好和事实知识
   - `/skills`：学到的技能和经验
   - 每份记忆都是独立的 Markdown 文件

5. **Sleep-time Agent**：
   - 主 Agent 只负责推理和回复（低延迟）
   - 每 5 步触发一次 Sleep-time Agent，后台读最近对话，分析更新 memory block
   - **三个好处**：主路径低延迟、后台可用更大 token 预算做深度反思、充分利用空闲时间

**评价**：开源里记忆自治能力最强的系统。但代价明显：认知成本高，数据库依赖重，80+ 依赖包。

---

### 4. Rainy — 文件级透明记忆（阿里 AgentScope）

**定位**：**把记忆的控制权和透明度还给用户**。

**核心理念**：传统做法记忆全存数据库，是黑盒；Rainy 直接存成 **Markdown 文件**，打开就能看见，可以直接编辑，可以 Git 版本控制。

**两套系统**：

| 系统 | 用途 | 时间维度 |
|------|------|---------|
| **REMILET** | 文件记忆，短期工作记忆 | 近期 |
| **本体** | 向量记忆，长期语义记忆 | 长期 |

**技术亮点**：

1. **Delta File Watcher 增量监控**：
   - 当记忆文件累积到几十 KB 后，每次变更都重新处理整个文件是对 Embedding API 的巨大浪费
   - Rainy 先检测文件是不是**纯追加模式**，如果是就只处理新增部分
   - **节省 92% 的 Embedding API 调用**

2. **Window-Use 和 Content 分离**：
   - 向量嵌入键在 `window-use` 上，而不是 `content` 上
   - 因为用户查询和 `window-use` 天然语义接近，**大幅提升召回率**

3. **AI 自主记忆管理**：
   - 把文件操作工具给 AI，让 Agent 自己决定如何组织记忆

**性能数据**：千问 3B + Rainy 后综合得分超过没有记忆的千问 34B —— **好的记忆系统可以让小模型打过大模型**。

**评价**：目前开源里最"对人友好"的记忆系统。来自阿里巴巴 **AgentScope** 团队。

---

### 5. memU — 记忆本身是一个 Agent

**定位**：**24 小时常驻、主动出击**的记忆系统。

**项目背景**：
- 由 **NevaMind-AI** 创建
- GitHub **~9.5K–14.4K stars**
- 开源协议：Apache 2.0（核心）/ AGPL-3.0（Bot）
- 支持 Codex、Claude Code、Cursor、OpenClaw 等主流 Agent 宿主

**核心思想**：前面四个项目的记忆系统都是**被动的** —— 用户说一句话，系统才 add 一条记忆；用户问问题，系统才检索一次。memU 想做的是**让记忆系统不再等待，自己主动跑**。

**双 Agent 架构**：

| Agent | 职责 | 特点 |
|-------|------|------|
| **Main Agent** | 听用户说话、调工具、生成回复 | 老本行 |
| **memU Bot** | 只负责记忆这一件事 | 持续盯着每次交互，后台提取/整理/分类 |

**实现方式**：memU Bot 用 Python 的 `asyncio` task 起一个异步后台任务，靠共享 `conversation_messages` 列表通信。设计精妙之处在于**简单到不像创新**。

**文件系统概念**：

| 概念 | 对应 |
|------|------|
| 数据库 | 底层存储 |
| 文件夹 | Category |
| 文件 | Memory Item |
| 符号链接 | 交叉引用 |
| 挂载点 | Resource |

> 与 Rainy 的用法完全不同：Rainy 是**给人看的**（透明文本），memU 的文件系统是 **Bot 自己维护的结构化目录树** —— 一个偏用户主权，一个偏 Bot 自治。

**显著性感知记忆（Citizens of Memory）**：
- 每条 memory item 带一个 `reinforcement_count` 计数器
- 每当被检索一次，计数 +1
- 排序时根据强化次数加权：**越常用的记忆越容易被再次召回**
- 模拟人类记忆的本质特性：你越经常想到的事情就越容易想起
- 可称为"给记忆加了**肌肉记忆**"

**性能数据**：
- **Locomo 基准**（原文称 OGMO，实际为 Locomo）所有推理任务平均准确率：**92.09%**
- 与 Mem0 用同一把尺子对比，给出非常有竞争力的数字

**三层架构**：

```
Resource Layer（原始数据仓库）
    ↓
Memory Item Layer（离散提取的记忆单元）
    ↓
Memory Category Layer（聚合的文本记忆单元）
```

**双模式检索系统**（经济可行的关键）：
- **Monitoring Mode**：轻量 Embedding 查找，用于日常观察（便宜）
- **Deep Reasoning Mode**：完整 LLM 调用，仅在需要真正理解时触发（贵但精准）
- **Token 成本降至同类 always-on Agent 的约 1/10**

**适用场景**：个人 AI 助手、企业级客服、DevOps Agent、研究型助手 —— 所有需要**长期陪伴、长期学习**的场景。

---

## 三、五项目对比总表

| 维度 | Text to Mem | Mem0 | Letta | Rainy | memU |
|------|:-----------:|:----:|:-----:|:-----:|:----:|
| **定位** | 操作语言标准 | 记忆中间件 | 记忆操作系统 | 透明文件记忆 | 主动记忆 Agent |
| **主动/被动** | — | 被动 | 半主动（Agent 自编辑）| 被动 | **主动（24/7）** |
| **存储形态** | SQLite（参考）| 向量+图 | 向量+PG+Git | **Markdown 文件** | Markdown 文件系统 |
| **用户可见性** | 低 | 中 | 中 | **高（完全透明）** | 中（Bot 自治）|
| **记忆管理权** | 开发者 | 系统 | **Agent 自己** | **用户** | **记忆 Bot** |
| **多 Agent** | 未明确 | ✅ 三层隔离 | ✅ Git tree 隔离 | 未明确 | ✅ 共享 memory pool |
| **成本特点** | — | 随记忆线性增长 | Token 开销高 | 省 92% Embedding | 省 90% Token |
| **上手难度** | 高（概念层）| 低（5 分钟接入）| 高（需理解三层架构）| 低 | 中 |
| **生产就绪** | 否（原型）| ✅ 强 | ✅ 强 | 中 | 中 |
| **最佳场景** | 定义标准 | 快速加记忆 | 长对话/个人助手 | 用户可控记忆 | 长期陪伴型 Agent |

---

## 四、核心设计模式提炼

### 模式 1：分层驱逐（Letta）
> 热数据（Core）→ 温数据（Recall）→ 冷数据（Archival），OS 虚拟内存思想。

### 模式 2：双路并行（Mem0）
> 向量存储（语义相似）+ 图存储（关系推理）并行跑，结果合并。

### 模式 3：职责分离 Prompt（Mem0）
> 用户记忆提取 vs 助手记忆提取分开，防止自我表达污染。

### 模式 4：增量处理（Rainy）
> 纯追加模式下只处理新增部分，避免全量重算。

### 模式 5：检索键分离（Rainy）
> Embedding 键放在 `window-use` 而非 `content`，匹配查询语义。

### 模式 6：显著性加权（memU）
> `reinforcement_count` 模拟人类"肌肉记忆"，常用记忆优先召回。

### 模式 7：双 Agent 架构（memU）
> Main Agent 负责交互，Memory Bot 负责后台记忆管理，互不阻塞。

### 模式 8：安全校验层（Text to Mem）
> 外层 Schema 校验 + 里层业务规则校验 + 危险操作 dry-run/confirmation。

---

## 五、一句话总结

| 项目 | 一句话 |
|------|--------|
| Text to Mem | **记忆操作应该有自己的指令集** |
| Mem0 | **给产品快速加记忆，选这个中间件** |
| Letta | **想做真正有状态的 Agent，看这个操作系统** |
| Rainy | **想要记忆透明可见、用户可控，选这个** |
| memU | **想要记忆能主动预测用户，看这个** |

> **最想留的思考**：从 *"agent 有记忆"* 到 *"记忆本身是一个 agent"* —— 这是 Agent 记忆系统正在发生的范式转移。