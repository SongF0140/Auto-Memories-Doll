以下是结合原项目和检索补充后的完整笔记。先纠正一个语音识别导致的混淆：**"Vicky" = LLM Wiki**，**"Lom/Lolm" = LLM**（大语言模型）。这是 Andrej Karpathy 在 2026 年 4 月提出的知识工程范式。

---

![LLM Wiki 架构图](sandbox:///mnt/agents/output/llm_wiki_architecture.png)

---

# LLM Wiki 知识工程范式笔记

## 一、项目背景

| 维度 | 内容 |
|------|------|
| **提出者** | Andrej Karpathy（OpenAI 联合创始人、前 Tesla AI 总监） |
| **发布时间** | 2026 年 4 月 3 日 |
| **形式** | GitHub Gist（非产品、非框架，是一个 **Pattern/范式**） |
| **影响力** | 首周 **5,000+ stars**，推文 **1,600 万+ 浏览量**，引发全球数十个独立开源实现 |
| **核心链接** | `gist.github.com/karpathy/442a6bf555914893e9891c11519de94f` |

> Karpathy 于 2026 年 5 月加入 Anthropic 预训练团队，但 LLM Wiki 作为独立范式继续演进。

---

## 二、核心思想：与 RAG 的根本区别

| | **RAG（检索增强生成）** | **LLM Wiki（知识编译）** |
|--|:--|:--|
| **驱动方式** | 查询驱动 | 摄取驱动 |
| **工作流程** | 每次提问 → 现场检索 → 现场拼凑 → 现场综合 → 回答完就散 | 新资料进来 → **立即编译** → 生成结构化知识页 → 查询时直接读编译结果 |
| **知识状态** | 无状态，每次从零重新发现 | 有状态，知识像**利息一样复利增长** |
| **成本结构** | 查询时付检索+生成成本 | 摄取时付编译成本（一次），查询时极便宜 |
| **适合场景** | 大规模召回、单次问答、实时检索 | 长期主题研究、知识沉淀、跨文档综合 |

> **关键洞察**：RAG 的昂贵部分不是"阅读"或"思考"，而是**簿记工作**（交叉引用、摘要、去重、矛盾解决）——这些恰好是 LLM 最擅长、人类最坚持不过两周的事。LLM Wiki 把工作量翻转：模型负责维护，人类负责策划和判断。

---

## 三、三层架构

Karpathy 最大的贡献是将知识系统干脆利落地切成三层，解决了"原始材料、派生内容、操作规则混在一起"的通病。

```
┌─────────────────────────────────────────────────────────┐
│  Layer 3: Schema 行为约束层 (CLAUDE.md / AGENTS.md)      │
│  ─────────────────────────────────────────────────────  │
│  • 页面怎么命名        • 什么时候新建页面                  │
│  • 怎么引用来源        • 遇到矛盾怎么处理                  │
│  • 没有 schema = 写作者；有 schema = 知识库维护者         │
├─────────────────────────────────────────────────────────┤
│  Layer 2: Wiki 派生知识层 (wiki/)                        │
│  ─────────────────────────────────────────────────────  │
│  • 概念页 / 实体页 / 对比页 / 综合页 / 矛盾页              │
│  • 可不断更新、链接、查询                                 │
│  • 是"被反复消化、交叉引用过的知识资产"                    │
├─────────────────────────────────────────────────────────┤
│  Layer 1: Raw Sources 原始材料层 (raw/)                  │
│  ─────────────────────────────────────────────────────  │
│  • PDF / 网页 / 录音 / 代码目录                            │
│  • 铁律：不可变。进了 raw/ 就不许回头改                    │
│  • 始终保持可追溯、可审计                                  │
└─────────────────────────────────────────────────────────┘
```

---

## 四、三个核心操作

| 操作 | 说明 | 成本特点 |
|------|------|---------|
| **Ingest（摄取/编译）** | 丢进一篇新资料，LLM 端到端阅读，更新所有相关 wiki 页（通常触达 10-15 个文件），更新 `index.md` 和 `log.md` | **成本集中在这里**。一篇 10 页 PDF 可能触发 12 个 wiki 文件更新，预算 ≈ **5-8× 原文 token** |
| **Query（查询）** | 问问题时，LLM 先读 `index.md`（轻量导航图），再读目标页，带来源输出答案 | 极便宜。若 wiki < ~100k tokens，检索可靠性 ≈ **100%**（无 chunk 遗漏） |
| **Lint（健康检查）** | 扫描矛盾、死链、孤儿页、事实漂移（source 说 X，wiki 说 Y）、覆盖缺口 | 类比知识库的 **CI/CD 管道**。建议每周或大批量摄取后运行 |

> 若 wiki 规模 < ~100 来源、几百页，光靠 `index.md` 作为内容目录就能出奇的好用。很多团队一上来就加向量库接 Graph RAG，反而搞复杂了。

---

## 五、页面类型（8-9 种）

| 类型 | 用途 |
|------|------|
| **source** | 单篇来源的摘要和出处 |
| **entity** | 人、公司、产品、库等对象 |
| **concept** | 术语、框架、方法、理论 |
| **comparison** | 横向对比 |
| **question** | 高价值问答的沉淀 |
| **synthesis** | 跨来源的综合结论 |
| **decision** | 决策和踩坑经验 |
| **gap** | 已知自己不知道的问题 |
| **meta** | 导航和控制面（index、log、overview 等）|

---

## 六、Front Matter：硬约束

每页必须有 YAML Front Matter，不是装饰品，是**硬约束**：

```yaml
---
type: concept          # 页面类型
title: "Redis Caching"
summary: "..."
sources: ["raw/2026-04-pdf.md"]
tags: [backend, cache]
status: active         # draft | active | archived
confidence: 0.85       # 置信度（v2 引入）
last_updated: 2026-04-15
---
```

没有 Front Matter，就无法做：
- 类型过滤
- 状态检测
- 图谱导出
- 结构化查询

更激进的实现（如 wiki-compiler）还在 Front Matter 里加了：
- `confidence`：置信度
- `provenance`：来源标记
- `state`：段落级状态（直接抽取 / 推断 / 有争议）
- `contributed_by`：谁贡献的

---

## 七、运行控制面（成熟系统的标志）

成熟的 LLM Wiki **不是有页面，而是有运行面板**：

| 文件 | 用途 |
|------|------|
| **index.md** | 内容目录。列出所有页面、分类、摘要。模型每次进来的第一个导航点 |
| **log.md** | 时间线。记录什么时候 ingest 了什么、跑了什么 lint、发起了什么 query |
| **overview.md** | 当前认知快照 |
| **hot.md** | 最近上下文缓存 |
| **purpose.md** | 约束这套 wiki 到底为什么存在 |
| **state.md** | 增量缓存 |
| **review_q.md** | 人工判断入口 |
| **graph.json** | 图谱导航 |

> Karpathy 认为，在约 100 个来源、几百个页面的规模下，`index.md` 作为内容目录居然还能继续出奇的好用。

---

## 八、Inject 管线（8 步）

一篇新资料进来，不是"写个摘要"就完事。成熟的 inject 管线大致有：

```
1. 接收原始材料
2. 规范化读取
3. Schema 分析
4. Pass 生成（概念抽取）
5. Pass 交叉引用
6. Pass 更新控制面
7. 进 review 队列
8. （可选）人工审核
```

**社区实现差异**：
- **wiki-compiler（桌面端）**：把 inject 硬拆成两次调用 —— 先做结构化分析，再生成页面和 review 项
- **atomic-memory-compiler**：把编译分成"概念抽取"和"页面生成"两个阶段，中间用 **SHA256 做 hash**，只有变了的地方才重编译 —— 这是整个范式里**最被低估的部分**，把生成工程化成了一条可增量、可缓存、可审计的流水线

---

## 九、社区演进：从 v1 到 v2 到 WiCER

### v1：Karpathy 原始 Gist（2026.04）
- Append-only ingest
- 手动 lint
- 人类可读的笔记

### v2：rohitg00 扩展（2026.03-04）
Gist 地址：`gist.github.com/rohitg00/2067ab416f7bbe447c1977edaaa681e2`

新增四个关键机制：

| 机制 | 说明 |
|------|------|
| **Confidence Scoring** | 每个事实带置信度分数：多少来源支持、多久前确认、是否有矛盾。置信度随时间衰减、随强化增强 |
| **Supersession（取代）** | 新信息 contradict 旧声明时，旧声明不删，但明确标记为被取代，带时间戳和链接。知识的版本控制 |
| **Ebbinghaus Decay** | 艾宾浩斯遗忘曲线，不同记忆类型有不同衰减率 |
| **四层固化管道** | 从原始 → 提取 → 综合 → 归档的渐进固化 |

> **核心分歧**：v2 选择 **overwrite（取代）** 旧声明；而另一派（如学术界的 Templated Substrate 论文）选择 **failure-path-preservation（保留失败路径）**，认为不应丢弃负面结果。

### WiCER：解决 Compilation Gap（2026.05）
论文：*WiCER: Wiki-memory Compile, Evaluate, Refine* (arXiv:2605.07068)

**问题**：盲编译（blind compilation）会灾难性丢失关键事实。

**数据**（17 个 RepLiQA 领域，6,800 个问题）：
- 全上下文 KV cache 在精选知识上优于 RAG（4.38 vs 4.08/5，TTFT 快 7.3×）
- 但在大规模原始文档上，注意力稀释导致质量低于 RAG
- **盲编译灾难性失败率：53-60%**（score-1 率），质量仅 2.14-2.32 vs 原始 3.46

**WiCER 算法**（灵感来自 CEGAR — 反例引导的抽象精化）：

```
Compile(D) → W₀
for t = 0 to T-1:
    Evaluate: 用诊断探针测试 Wₜ
    Diagnose: 找出编译器丢掉的关键事实
    Recompile: 把关键事实作为"必须保留"约束，重新编译 → Wₜ₊₁
```

**结果**：
- 1-2 轮迭代恢复 **80%** 丢失质量（3.24 vs 3.47）
- 灾难性失败率从 **55.1% → 24.8%**（相对降低 55%）
- 消融实验：targeted diagnosis 贡献 **+0.95**，random pinning 仅 **+0.16** —— **精准诊断才是价值所在**

### Ghelbur 重建版（2026.05）
在 v2 基础上新增：
- **Scheduled Agents**：定时自动运行（非手动触发）
- **Automatic Synthesis**：对话中的高价值判断自动沉淀为 wiki 页
- **AI-First Note Structure**：笔记不是给人读的，是给未来 AI 读的

> **AI-First Vault Principle**：Front Matter 告诉 AI 这是什么类型的笔记；"For future Claude" 前言告诉它是否值得读；recency markers 告诉它要验证什么；wikilinks 告诉它下一步去哪。一旦停止假装你会重读笔记，你就可以为真正会读它们的系统去写。

---

## 十、四大进阶机制

### 1. Crystallize（结晶）
> 如果一次对话里做出了有长期价值的综合判断，它**不应该只存在聊天记录**，而应被沉淀成新的 synthesis/comparison/question 页。

**不做这一步，LLM Wiki 就会退化为一个带日志的聊天系统。**

### 2. Link Intelligence（链接检查）
分两层：
- **结构性检查**（health.py）：死链、孤儿页、重复命名、Front Matter 缺失
- **语义性检查**（linked.py）：成就声明、冲突结论、可疑推断

> 分层是聪明的工程决定：前者**不调 LLM**（便宜），后者才调。

### 3. Research（主动研究）
LLM Wiki 不只是被动消化，还会**主动发现自己的知识缺口**：
- 从 `graph.json` insights 或 `review_q` 里自动生成研究主题
- 派出搜索 query
- 把结果回写进 wiki

### 4. 对抗性 Inject（Adversarial Ingest）
大部分实现是串行单视角的：一篇资料进来，读一遍写一遍。最大问题是**确认偏差**。

**NVK-LLM-Wiki 实现给出两种对抗模式**：

| 模式 | 机制 | 输出 |
|------|------|------|
| **Faces 模式** | 对有争议的命题，派 5-10 个立场分化的 agent 并行取证（支持方、反对方、机制派、综述派、相关领域派） | 明确 verdict：`supported` / `partially supported` / `contradicted` / `insufficient evidence` / `mixed` |
| **多角度并行采集** | 同一主题分学术/技术/应用/新闻/反主流五路并行 inject，再让 link 阶段处理冲突 | Token 和延迟翻几倍，但高风险结论更可靠 |

---

## 十一、检索层：当 wiki 变大时

Karpathy 自己承认，当规模变大时，光靠 `index.md` 不够。社区共识是**三路信号融合**：

| 信号 | 作用 | 适用场景 |
|------|------|---------|
| **BM25** | 词法匹配 | 精确术语、专有名词 |
| **Vector** | 语义匹配 | 同义表达、概念关联 |
| **Graph** | 关系建立 | 多跳推理、跨实体关联 |

合并：Reciprocal Rank Fusion (RRF)

**重要判断**：Graph RAG 不是万能药。评测显示，Graph RAG 在**多跳推理、跨实体关联、全局摘要**上有优势，但在**单跳事实检索**上，传统 RAG 更简洁高效。

正确路线：**Adaptive RAG** —— 先用 query classifier 判断问题是 single-hop / multi-hop / 主题总览，再路由到对应检索管线。RAG / Graph RAG / LLM Wiki 三者不是互斥替代，是**叠加关系**。

---

## 十二、与相关技术的关系

| 技术 | 关系 | 区别 |
|------|------|------|
| **RAG** | 互补，非替代 | RAG 适合大规模召回和单次问答；LLM Wiki 适合长期沉淀 |
| **Agent Memory** | 不同层 | Agent Memory = 过程记忆（这次 agent 做了什么、踩过什么坑）；LLM Wiki = 知识资产层（对外可读、长期维护的结论） |
| **Obsidian / Notion** | 不同层 | PKM 工具是前端容器；LLM Wiki 的关键是背后的 schema、inject 管线、review 工作流 |
| **知识图谱** | 共存 | 图谱适合做导航、聚类、桥接发现；页面适合做解释、叙事、引用。成熟路线是 **graph + page 共存** |

---

## 十三、风险与坑

### 🔴 核心风险：幻觉回写的自我强化闭环
> Inject 时会读自己以前写过的 wiki 页面当背景知识。**一次幻觉一旦被保存，会在后续 inject 中被当作事实继续扩展、交叉引用、写入新页面** —— 这是一个会自我强化的错误传播链，比单次幻觉危险得多。

**缓解手段**（非根治）：
- 段落级 `provenance` 标记（直接抽取 / 推断 / 争议）
- `confidence` + `supersession` 定期重新 link
- 高风险声明主动送进 `review_q` 让人判

### 🟡 衍生问题
| 问题 | 说明 |
|------|------|
| **上下文盲区** | 一次 inject 通常只让 LLM 看到 index + 少量相关页，不是整本 wiki。经常以不知情状态改已存在的事实，产生局部一致但全局矛盾 |
| **不可逆信息损失** | 专业化会压掉细粒度的条件、例外、限定语，之后找不回来 |
| **去出处化** | 若生成页不带原句引用，事后有疑问无法回溯 |

### 🟡 维护成本转移
> LLM Wiki **不是让维护成本消失了，而是把成本从一个地方搬到了另一个地方**。原来你要花时间写笔记、整理链接；现在你要花时间维护 schema、审 review、看 link 报告。

---

## 十四、适用场景

| ✅ 适合 | ❌ 不适合 |
|--------|----------|
| 长期主题研究 | 一次性问答 |
| 论文精读 | 极大规模低价值语料粗筛（先上 RAG） |
| 代码库理解 | 实时监控告警 |
| 竞品情报追踪 | 严格合规的企业知识平台（还缺基础设施） |
| 个人第二大脑 | |
| 多 Agent 共享上下文 | |

---

## 十五、评价指标（知识资产健康度）

| 指标 | 含义 |
|------|------|
| **Source Coverage** | 原始资料有没有被覆盖 |
| **Index Freshness** | 索引是否及时更新 |
| **Citation Coverage** | 关键结论有没有证据 |
| **Review Backlog** | 待审问题有没有越堆越多 |
| **Query → Page** | 高价值问答有没有真的沉淀回 wiki |

---

## 十六、核心洞察总结

> **LLM 是编译器，聊天是入口，wiki 是产品，graph 是导航层，schema 是操作系统，review 是刹车，log 是审计轨迹。**

LLM Wiki 的真正价值不在任何一次回答听起来多聪明，而在于一件更朴素的事：

> **它让每一次 ingest、每一次 query、每一次 research，都变成了对同一份知识资产的增量投资。**

从"每次重新发现"到"知识复利增长" —— 这是 LLM Wiki 试图完成的范式转移。