/**
 * 记忆搜索组件
 *
 * 调用关系：
 * - 被引用：components/memory/MemoryViewer.tsx
 * - 调用：features/memory/* （搜索记忆）
 * - 调用：lib/vector/* （向量检索）
 *
 * 作用：
 * - 提供记忆搜索界面
 * - 支持关键词搜索、向量检索、标签过滤
 * - 展示搜索结果和排序
 * - 处理搜索回写（点击次数、访问时间）
 */