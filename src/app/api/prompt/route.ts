/**
 * 提示词更新入口 API 路由
 *
 * 调用关系：
 * - 接收：前端 POST 请求（提示词更新）
 * - 调用：features/prompt/* （提示词读写与回写）
 * - 调用：lib/prompt/* （提示词模板与处理）
 * - 调用：lib/storage/* （本地存储与文件写回）
 *
 * 作用：
 * - 处理提示词的读取和更新操作
 * - 管理个性标签和偏好参数
 * - 同步更新 profile.md
 *
 * 约束：
 * - 请求体 schema：需要定义
 * - 响应体 schema：需要定义
 * - 错误码表：需要定义
 */