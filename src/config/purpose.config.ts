/**
 * 系统存在目的（I-5 控制面 · purpose.md 静态配置）。
 *
 * LLM Wiki §控制面：purpose.md 回答"本系统为什么存在"，是导航的最终锚点——
 * 模型（和人）在迷失于细节前，先读这一页校准方向。内容是设计决策的沉淀，
 * 手工维护、随架构演进而更新，不参与自动生成。
 */
export const SYSTEM_PURPOSE = `# 系统目的（purpose）

> 本文件回答"这个记忆系统为什么存在"。模型与维护者在深入任何细节前，先读这一页。

## 为什么存在

个人开发者日常产生大量碎片知识——调试记录、选型结论、踩坑笔记，散落在
Cursor / Claude Code 会话、笔记文件与对话里。人脑记不全，全文搜索又找不回
"当时为什么这么做"。本系统把这些碎片自动整理成**中文可读的知识卡**，
在对话时按需注入，让"过去的自己"持续为"现在的自己"工作。

## 系统做什么

1. **采集**：监听笔记目录与编码工具会话，自动抽取记忆卡片（一个话题一张卡）。
2. **整理**：全部卡片重写为简体中文，带标题、摘要、标签、使用场景（windowUse）。
3. **检索**：按问题类型路由（单跳事实 / 多跳关联 / 总览 / 时序），向量 + 关键词 RRF 融合召回。
4. **演进**：冲突不覆盖而是取代链追溯；同话题卡片夜间编译成综合结论（synthesis）。
5. **自我维护**：结构 Lint、置信度衰减、编译探针验证——夜里自己长出结论，白天保持可信。

## 边界与原则（不可破坏）

- accessCount 只在用户主动点击时增加，自动检索不污染热度。
- 卡片全部中文可读；推断内容（inference）降权并标注，不与已验证事实同权。
- memory.db 是唯一真源，Markdown 是它的投影；系统元数据不进入采集管线。
- 检索相似度阈值 0.3，宁缺毋滥。
`.trim();

/** 控制面 purpose.md 的文件名常量（与 index.md 等同目录，memory-root 下） */
export const PURPOSE_FILE_NAME = "purpose.md";
