# 记忆系统改进指南

> **依据**：[记忆系统相关.md](记忆系统相关.md)（五范式：Text to Mem / Mem0 / Letta / Rainy / memU）+ [llmwiki相关.md](llmwiki相关.md)（Karpathy LLM Wiki 范式）+ 代码实况审计（2026-09-04）
>
> **核心诊断**：我们是一套"带热度治理的 RAG 记忆库"，地基（向量+图+审计+多源监听）完备，但缺三样东西——**编译层**（知识不增值）、**控制面**（模型无导航点）、**验证闭环**（幻觉可回写）。所有改进都生长在现有管线上，不需要推倒重来。

---

## 一、现状快照

### 1.1 现有架构

```
写入路径（查询驱动，被动）                     治理路径（夜间批处理，T+1）
──────────────────────────                  ──────────────────────────
file-watcher / tool-dir-watcher             nightly-orchestrator（旗舰 LLM）
tool-dir-watcher / mcp / browser              ├─ contradiction-detector  矛盾检测
        │                                     ├─ link-supplementer       补 wikilink
        ▼                                     ├─ route-optimizer         路由建议
  json-pipeline → splitter → quality-filter   ├─ profile-updater         画像更新
        │                                     └─ daily-reporter          日报
        ▼
  orchestrator（抽卡，≤8 卡/源）              调度器：audit / cleanup / retention
        │                                              / vector / nightly
        ▼
  memory-extraction-service                  存储层
        │                                    ─────────────
        ▼                                    memory.db（better-sqlite3, WAL, 真源）
  MemoryRecord（全中文卡）                      ├─ vector_records（usearch HNSW）
        │                                     └─ conflict_records（冲突三态）
        ├─→ Markdown 落盘（notes/）           notes/（透明可编辑）
        └─→ 向量化（generator → VectorIndex）
                                             检索路径
审计工作台（人工裁决）                        ─────────
──────────────────────                       query → 规则式 rewriter/expansion
audit-worker → conflict_records                    → VectorRetriever（0.3 阈值）
        │                                          ├─ vector 主路（HNSW）
        ▼                                          └─ keyword 降级路（embedding 失败时）
  accept / keep / manual                      → Ranker（多因子 + MMR α=0.7）
                                               → 注入 chat context
```

### 1.2 已有资产清单（不动的地基）

| 资产 | 位置 | 说明 |
|------|------|------|
| 向量双后端 | `src/lib/vector/backend.ts` | usearch HNSW 主路 + JS 精确扫描兜底 |
| MMR 多因子重排 | `src/lib/vector/ranker.ts` | relevance/heat/recency/access/tagAffinity |
| 关键词索引 | `src/lib/vector/keyword-index.ts` | 已存在，未参与融合 |
| 记忆关联图 | `src/lib/graph/wiki-graph.ts` | 已接入 chat-handler，未参与检索主路 |
| MemoryKind 闸门 | `src/types/memory.ts` | fact/inference/hypothesis/insight，非 fact 强制人工 |
| 冲突三态裁决 | `src/server/services/audit-service.ts` | resolved_accept / keep / manual |
| 版本快照类型 | `src/types/memory.ts` MemoryVersion | 类型已定义，机制未落全 |
| 深夜督查 | `src/server/orchestrators/nightly-orchestrator.ts` | 旗舰 LLM 驱动，每天一次 |
| 多源监听 | `src/server/watchers/` + `listener-service` | 文件/工具目录/MCP/浏览器 |
| 审计工作台 UI | `src/app/(main)/audit/` | 人工裁决入口 |
| evals 框架 | `evals/` | 自建测试，无外部基准 |

---

## 二、缺陷总表

| 编号 | 缺陷 | 证据 | 命中的范式批评 | 批次 |
|------|------|------|----------------|------|
| P0-1 | 无结构化 Lint（死链/孤儿页/front-matter 残缺） | 无对应模块 | LLM Wiki §Lint | 一 |
| P0-2 | 幻觉回写闭环无降权（chat 推断内容与事实同权） | kind 闸门存在但 chat 源默认行为待强约束 | LLM Wiki §核心风险 | 一 |
| P1-1 | 冲突裁决 = 丢弃旧记忆，无 Supersession 取代链 | audit-service resolved_accept 直接覆盖 | LLM Wiki v2 | 二 |
| P1-2 | 无 confidence / status 字段 | MemoryRecord 缺失 | LLM Wiki v2 | 二 |
| P1-3 | 无控制面（index/overview），模型无导航点 | 无对应文件 | LLM Wiki §控制面 | 二 |
| P2-1 | 无 Synthesis 编译层，知识不复利 | nightly 只检查不合成 | LLM Wiki §Crystallize + memU 聚合层（双范式点名） | 三 |
| P2-2 | 编译无验证（WiCER：盲编译 53-60% 灾难性失败） | — | WiCER 论文 | 三 |
| P2-3 | 检索不路由、三路信号未融合（vector/keyword 是降级不是融合，graph 不在主路） | `retriever.ts` L47-58 | Adaptive RAG | 三 |
| P3-1 | 强化回路太慢：accessCount 仅用户点击，冷启动死循环 | 项目约束（防污染决策） | memU reinforcement | 四 |
| P3-2 | 增量是文件级不是段级（变更即全文件重嵌） | `file-watcher.ts` sourceHash 跳过 | Rainy Delta（省 92%） | 四 |
| P3-3 | Embedding 键在 content 而非 window-use | `generator.ts` | Rainy 检索键分离 | 四 |
| P3-4 | 无可比基准成绩单 | evals/ 自建 | memU Locomo 92% | 四 |

---

## 三、改进项详解

### 批次一：速赢（零/低成本，一周内）

#### I-1 结构化 Lint（纯规则，不调 LLM）

**原理**：LLM Wiki 的 Link Intelligence 分两层——结构检查便宜（不调 LLM），语义检查才调。我们夜里已经在调 LLM 做语义层，缺的是便宜的结构层。

**现状**：无任何死链/孤儿检测；`graphLinks` 指向的记忆被删除后成为悬挂引用。

**方案**：新增 `src/server/services/structure-lint-service.ts`，纯规则扫描：

```typescript
type LintIssue =
  | { type: "dead-link"; from: string; to: string }        // graphLinks 指向不存在的 id
  | { type: "orphan"; memoryId: string }                    // 无 graphLinks 且 topic 下无邻居
  | { type: "incomplete-card"; memoryId: string; missing: string[] } // title/summary/tags/topic 空
  | { type: "duplicate-hash"; memoryIds: string[] };        // evidence.sourceHash 重复入库

export class StructureLintService {
  /** 全量规则扫描，不调用任何 LLM */
  run(all: MemoryRecord[]): LintIssue[] { /* ... */ }
}
```

**接入点**：nightly-orchestrator 新增第 6 步；结果写入审计工作台（复用 conflict_records 表或新建 lint_issues 表）。

**验收**：构造含死链/孤儿的测试数据集能全部检出；单次扫描 < 1s（千级记忆）；不产生任何 LLM 调用。

#### I-2 幻觉回写防护补全

**原理**：LLM Wiki §核心风险——一次幻觉被保存后会在后续 inject 中被当事实继续扩散。我们恰好踩雷：chat 在 Memory 模式注入检索结果 → 生成内容 → 以 `sourceType: "chat"` 被 ingest 抽卡 → 下次再注入。

**现状**：`MemoryKind` 闸门已存在（非 fact 强制人工裁决），但需确认 chat 源抽取的推断性内容是否稳定标记为 `inference`；且 ranker 对非 fact 无降权。

**方案**（三处小改动）：
1. 抽取侧：`sourceType === "chat"` 且无法给出原文 evidence 的候选，强制 `kind = "inference"`（闸门自动转人工，复用 PendingEvent 的 `review` 状态）
2. 排序侧：`computeBaseScores` 增加 kind 因子——`fact ×1.0，inference ×0.85，hypothesis ×0.7`，防止推断内容与已验证事实同权
3. 注入侧：chat context 拼接时对非 fact 卡片追加来源提示（"以下为 AI 推断，未经证实"）

**验收**：chat 推断卡默认进 review 队列；注入 prompt 中推断卡带标记；ranker 单测覆盖 kind 权重。

---

### 批次二：schema 演进（第 2-3 周）

#### I-3 Supersession 取代链

**原理**：v2 的关键机制——新信息 contradict 旧声明时，旧声明**不删**，标记 `supersededBy` 带时间戳。知识需要版本历史，否则"曾经以为 X 后来发现是 Y"的轨迹消失，下次矛盾检测还可能翻旧案。

**现状**：`ConflictRecord` 裁决为 `resolved_accept` 时，新值直接覆盖旧值（旧记忆丢失）。`MemoryVersion` 快照类型已定义未落全。

**方案**：
1. `MemoryRecord` 增加字段（`MEMORY_VERSION` bump，走 `storage-migration-service`）：

```typescript
/** 生命周期状态（缺省视为 active） */
status?: "active" | "superseded" | "archived";
/** 被哪条记忆取代（status=superseded 时必有） */
supersededBy?: string;
/** 取代了哪条记忆（新卡回指，构成链） */
supersedes?: string;
```

2. 裁决流改造：`resolved_accept` 时旧记忆 → `status: "superseded"` + 写快照（补全 MemoryVersion 机制）；新记忆写入 `supersedes`
3. 检索侧：默认注入只取 `status !== "superseded"`；审计 UI 增加取代链可视化（A → B → C 时间线）
4. 矛盾检测侧：跳过 superseded 卡片，避免翻旧案

**验收**：冲突裁决后旧卡可追溯；superseded 卡不进注入但可在记忆库页查看链路；migration 对存量数据幂等。

#### I-4 Front Matter 富化（confidence / status）

**原理**：LLM Wiki v2——置信度随时间衰减、随强化增强；没有 Front Matter 约束就无法做类型过滤、状态检测、图谱导出。

**方案**：
1. `MemoryRecord` 增加 `confidence?: number`（0-1）
2. 初始值 = quality-filter 质量分 × 证据系数（有 sourceHash 的 fact 起步 0.9，推断卡 0.5）
3. 衰减：retention-scheduler 每次运行时按 recency 半衰期（复用 λ=0.01）折减
4. 强化：用户主动点击（access 事件）时 confidence +0.05，封顶 0.95
5. ranker 把 confidence 纳入 heat 因子计算（替代裸 heatScore 的部分权重）

**验收**：confidence 随时间和访问双向变动有单测；Markdown front matter 同步写入（保持文件与 DB 一致）。

#### I-5 控制面：index.md / overview.md

**原理**：LLM Wiki §控制面——成熟系统不是有页面，而是有运行面板。模型进来的**第一个导航点**是 index.md。约 100 来源规模下，一个内容目录就能出奇地好用。

**方案**：
1. `nightly-orchestrator` 的 daily-reporter 扩展生成两个文件到 memory-root：
   - `index.md`：topic 列表 × 卡片数 × 最近更新时间 × 一句话摘要
   - `overview.md`：各 topic 的认知快照（当前结论、已知矛盾、开放问题）
2. chat 的 system prompt 头部注入截断版 index（预算 ≤ 500 token，超限按热度取 top topics）
3. purpose.md（本系统为什么存在）先以静态配置形式放在 `src/config/`，不急

**验收**：chat 请求的 prompt 中可见 index 头部；index.md 由夜跑自动刷新；token 预算有硬上限。

---

### 批次三：编译层（第 4-6 周，夜间算力）

#### I-6 Synthesis 编译（最高价值项）

**原理**：这是 **memU 聚合层 + LLM Wiki Crystallize 的双重缺口**——两个独立范式点名同一缺陷。当前 20 张同 topic 卡片永远不会被编译成一页综合结论，知识存一年还是碎片。

**方案**：
1. `MemoryKind` 扩展：`"fact" | "inference" | "hypothesis" | "insight" | "synthesis"`
2. nightly-orchestrator 新增编译任务（插在矛盾检测之后，可复用其结论）：

```
同 topic 卡片聚类（topic + graph 社区检测，复用 wiki-graph）
    ↓
筛选可编译簇：≥5 张卡 && 近 14 天有更新 && 非全 superseded
    ↓
旗舰 LLM 编译 → synthesis 卡（引用来源卡 id，写入 sources 字段）
    ↓
WiCER 验证（I-7）→ 通过则落盘 + 向量化
    ↓
来源卡标记 synthesizedBy（不删，被综合≠被取代）
```

3. synthesis 卡落盘到 `notes/synthesis/<topic>/`，与人工笔记隔离
4. 存量 synthesis 卡每次夜跑检查"来源卡变更 → 重新编译"（增量缓存，参考 atomic-memory-compiler 的 SHA256 思路）

**验收**：造一个 ≥5 卡的 topic 跑夜跑能产出 synthesis 卡；来源变更触发重编译；synthesis 卡在 UI 有明确类型标识。

#### I-7 WiCER 式编译验证

**原理**：WiCER 论文数据——盲编译灾难性失败率 53-60%；用"诊断探针→精准重编译"1-2 轮可恢复 80% 丢失质量，失败率降到 24.8%。**做编译必须带验证，audit 管线正好是现成的刹车。**

**方案**（接在 I-6 之后）：

```
编译产物 W₀
    ↓
生成诊断探针：对每张来源卡的核心事实生成 1 个探针问题（便宜模型）
    ↓
用 W₀ 回答探针 → 比对来源卡 summary
    ↓
命中失败：把失败事实作为"必须保留"约束，重新编译 → W₁（至多 2 轮）
    ↓
仍失败 → 该簇进 review 队列人工裁决（不落盘）
```

**验收**：探针测试覆盖率 100% 来源卡；2 轮重编译有硬上限（防夜间预算失控）；失败簇可见于审计页。

#### I-8 自适应检索路由 + RRF 三路融合

**原理**：LLM Wiki §检索层——RAG / Graph RAG / LLM Wiki 是**叠加关系**。我们三路零件齐全（vector / keyword-index / wiki-graph）却是"vector 主路 + keyword 降级"，graph 完全不在查询主路上。

**现状证据**：`retriever.ts` L47-58——embedding 为空才切 keyword（降级，非融合）。

**方案**：
1. 新增 `src/lib/vector/query-classifier.ts`（**规则式起步，不调 LLM**）：

```typescript
type QueryRoute = "single-hop" | "multi-hop" | "overview";
// 规则信号：对比类词（区别/对比/相比/优劣）→ multi-hop
//          总览类词（总结/概览/所有/梳理）+ topic 命中 → overview
//          其余 → single-hop
```

2. 三路融合（RRF，k=60）：

```
single-hop：RRF(vector, keyword)
multi-hop：vector 取种子 → wiki-graph 扩 1 跳邻居 → RRF(vector, graph)
overview：直接返回 topic 的 synthesis 卡 + index.md 片段（不打向量）
```

3. 路由结果记入检索响应（`mode` 字段已有，扩展枚举），便于后续统计调优

**验收**：三类典型 query 各走对各的路；RRF 融合单测；overview 路径零 embedding 调用。

---

### 批次四：检索质量与反馈（第 7 周起，按需插排）

#### I-9 双信号热度（弱/强分离）

**原理**：memU 的 reinforcement_count（每次检索 +1）会被自动召回灌水；我们的 accessCount 仅用户点击（防污染决策）导致冷启动死循环。**中间态**：两种信号都记，权重分开。

**方案**（不破坏既有决策）：
1. `MemoryRecord` 增加 `retrievalCount?: number`（自动检索弱信号，每次被注入 +1）——**accessCount 语义不动，红线不破**
2. ranker 的 access 因子改为加权组合：

```
accessScore = 0.3 × norm(log(1+retrievalCount)) + 0.7 × norm(log(1+accessCount))
```

3. retrievalCount 有日衰减（防历史高频卡永久霸榜），accessCount 不衰减

**验收**：纯自动检索不改变 accessCount；冷启动卡可通过弱信号进入候选池；热度灌水回归测试通过。

#### I-10 段级增量嵌入（Delta）

**原理**：Rainy 检测纯追加模式只嵌新增段，省 92% Embedding。我们的监听对象（Cursor/Claude Code 日志、笔记）恰是典型追加型文件。

**现状**：`file-watcher.ts` 用 `evidence.sourceHash` 跳过未变更文件（对），但**变更即整文件重新走管线**。

**方案**：
1. 变更时先判追加：`新内容.startsWith(旧内容)` 且旧 hash 是新内容前缀的 sha256 → 纯追加
2. 追加场景：只把 delta 文本送入抽取管线，产出新卡（旧卡不动）
3. 非追加（编辑/重写）：保持现状全量处理
4. 在 `listen-stats-service` 记录节省比例，可量化收益

**验收**：追加型大文件变更只产生 delta 卡；编辑型文件行为不变；节省率报表可见。

#### I-11 window-use 检索键分离

**原理**：Rainy——用户查询和"这条记忆什么时候有用"天然语义对齐，把 Embedding 键放在使用场景而非内容本身，召回率从源头提升。我们的规则式 query-rewriter 是下游补偿，Rainy 是上游根治。

**方案**：
1. 抽卡时 LLM 额外生成一行 `windowUse`（"当用户问 X / 做 Y 时这条记忆有用"）
2. embedding 生成键改为 `summary + windowUse`（不再用全文）
3. **注意**：这改变所有向量的输入分布 → 必须全量重嵌，需在 `/api/memory/rebuild` 里做成一次性迁移，且按 memoryId 分批限流

**验收**：迁移后抽样对比 20 组典型 query 的召回 top-10 变化；全量重嵌可断点续跑。

#### I-12 Locomo 基准评测

**原理**：memU 敢报 Locomo 92.09%，我们没有尺子就无法证明任何改进有效。**这是所有改进的"对照组"来源，宜早不宜迟——但可以和批次一并行做最小版。**

**方案**：
1. `evals/` 下新增 Locomo 风格数据集（多轮对话 → 事后问答，含单跳/多跳/时序推理）
2. 三组对照：裸模型（无记忆）/ 现行 RAG 管线 / 改进后管线
3. `evals/reports/` 沉淀每次跑分，作为批次三上线的验收依据

**验收**：一条命令跑完三组对照并出报告；批次三每个改进项上线前后跑分可比。

---

## 四、实施顺序与依赖

```
M1（第 1 周）          M2（第 2-3 周）         M3（第 4-6 周）           M4（第 7 周+）
┌─────────────┐      ┌─────────────┐        ┌──────────────┐         ┌──────────────┐
│ I-1 结构Lint │      │ I-3 取代链   │──────→ │ I-6 Synthesis │──────→  │ I-9 双信号   │
│ I-2 幻觉防护 │      │ I-4 置信度   │        │ I-7 编译验证  │         │ I-10 段级增量 │
│ I-12 基准(最小版,并行)│ │ I-5 控制面   │──────→ │ I-8 自适应路由 │←───────│ I-11 检索键   │
└─────────────┘      └─────────────┘        └──────────────┘         └──────────────┘
       │                     │                      │
       └── I-2 的 review 队列 ──→ I-3 的裁决流复用    └── I-8 的 overview 路依赖 I-6 的产物
```

**关键依赖**：I-6 依赖 I-3（synthesis 来源卡需要 status 语义）；I-8 的 overview 路由依赖 I-6 产物和 I-5 的 index；I-12 最小版越早越好，它是 M3 的验收尺子。

---

## 五、红线：不可破坏的既有决策

| 红线 | 说明 | 本指南如何兼容 |
|------|------|----------------|
| accessCount 仅用户主动点击 | 防止自动检索污染 heatScore | I-9 新增独立字段，不动 accessCount 语义 |
| 向量检索 0.3 相似度阈值 | 过滤低相关噪声 | I-8 各路由保留阈值参数 |
| MMR 默认 α=0.7 | 多样性重排既有默认 | I-8 融合发生在 ranker 之前，MMR 保持不变 |
| 卡片全中文可读 | 项目硬约束 | synthesis 卡同样全中文；windowUse 字段双语 |
| ≤8 卡/源 topic 拆分 | 既有拆卡规则 | 不触碰 |
| memory.db 为真源 | SQLite WAL 真源架构 | 所有新表同库，不引入新存储引擎 |
| 系统元数据不入库 | file-watcher 排除 topics.md 等 | I-5 的 index.md/overview.md 必须加入排除名单 |

---

## 六、健康度指标面板（上线后持续观测）

| 指标 | 含义 | 目标 |
|------|------|------|
| Lint 通过率 | 死链/孤儿/残缺卡占比 | 死链 = 0，孤儿 < 5% |
| Review Backlog | 待人工裁决堆积量 | 不随规模线性增长 |
| Supersession 链长 | 平均被取代次数 | 反映知识更新活跃度 |
| Synthesis 覆盖率 | 有 synthesis 卡的活跃 topic 占比 | M3 后 > 60% |
| 编译探针通过率 | WiCER 验证一次通过比例 | > 80%，低则调编译 prompt |
| 路由分布 | single/multi/overview 占比 | 各路由召回质量分别评估 |
| 三组基准分 | 裸模型 / 现行 RAG / 改进后 | 改进后相对现行 RAG 提升可复现 |
| Embedding 节省率 | delta 处理节省的调用 | 追加型来源 > 80% |

---

## 七、一句话总结

> **先把刹车装好（Lint + 幻觉防护），再给知识加上版本与信用（Supersession + confidence），然后让系统在夜里自己长出结论（Synthesis + WiCER 验证），最后把每次查询、每次编译都变成对同一份知识资产的增量投资。**
>
> 从"带热度治理的 RAG 记忆库"到"会自己长出结论的知识编译器"——这就是本指南要走的路。

---

## 附录：阶段二实施记录（2026-09-14）

本批次落地 **I-8（temporal 路由）+ I-11（windowUse 检索键对齐）+ I-12（评测接线）**，针对 Locomo 跑分暴露的时序短板（temporal 组 Recall@1 仅 0.67）。

### A.1 变更清单

| 文件 | 变更 |
|------|------|
| `src/lib/vector/query-classifier.ts` | 新增 `temporal` 路由类型与 `TEMPORAL_PATTERNS`（哪一天/最后一次/什么时候等），优先级高于 multi-hop/overview |
| `src/lib/vector/temporal.ts`（新建） | 纯函数集合：`extractTemporalAnchor`（解析"在 X 之前/之后"）、`stripTemporalClause`、`extractDate`（ISO/斜杠/中文日期归一化）、`rankTemporal`（锚定方向过滤 + 相似度并列时新者优先） |
| `src/lib/vector/retriever.ts` | 新增 `temporalMetaProvider` 选项与 `searchTemporal` 分支：锚定子句剥离 → 意图查询双路 RRF → 锚文本反查锚定日 → 方向过滤 + 时间感知排序；解析失败退化常规检索 |
| `src/features/chat/handler.ts` | 生产接线：`temporalMetaProvider` 从 MemoryService 批量取 createdAt + 正文 |
| `src/eval/fixtures.ts` | 全部 24 条记忆补 `windowUse` 字段，embedding 键与生产对齐 |
| `src/eval/locomo-eval.test.ts` | improved 臂注入 `temporalMetaProvider`，与生产 handler 接线一致 |
| `src/__tests__/temporal-retrieval.test.ts`（新建） | 19 个用例：classifier 路由、锚定解析、日期提取、时间感知排序、retriever 集成（含"锚定之后的高相似度干扰项被淘汰"关键场景与无 provider 退化路径） |

### A.2 验收数据（重生报告 evals/reports/locomo-eval-report.md）

| 对照臂 | Recall@1 | Recall@5 | Recall@10 | MRR |
| --- | --- | --- | --- | --- |
| bare | 0 | 0 | 0 | 0 |
| baseline | 0.7 | 0.9 | 1 | 0.8167 |
| improved | **1** | **1** | **1** | **1** |

分组对比（baseline → improved）：

- temporal：Recall@1 **0.33 → 1.0**，MRR 0.5556 → 1.0（本批次核心目标，达成）
- single-hop：Recall@1 0.75 → 1.0（windowUse 键对齐的附带收益）
- multi-hop：维持 1.0（无回退）

### A.3 设计要点（为什么这样做）

1. **锚定剥离**："在决定用 Zustand 之前，最后一次调整部署平台"——Zustand 是时间参照物而非检索目标，不剥离会成为相似度干扰项（正是 baseline temporal 失分的根因）。
2. **宽松过滤**：无日期元数据的候选不误杀；方向过滤清空时回退未过滤集合——宁可宽松也不空手而归。
3. **零 LLM 成本**：锚定解析、日期提取、时间排序均为纯规则/纯函数，路由开销不吃掉融合收益。
4. **退化安全**：未注入 metaProvider、锚定日无法解析、无候选时均退回常规混合检索，功能不缺失（红线兼容）。

---

## 附录二：收尾阶段实施记录（2026-09-14）

指南 12 项全部落地后的**上线收尾**：I-11 存量迁移执行、健康度面板路由分布、I-5 purpose.md 遗留项。至此指南范围内无剩余代码缺口。

### B.1 生产库体检结论（迁移前）

- 生产库 `memory-root/memory.db`：55 条记忆，**全部缺 windowUse**，55 条向量均为旧 content 键
- `vector_rebuild_state` 为空 → 迁移从未开始
- 关键发现：**直接重嵌是陷阱**——`buildEmbeddingKey` 缺 windowUse 时回退 content 键，重嵌等于原样重算，白烧 55 次 embedding 配额且键分布不变。迁移必须"先回填 windowUse，再重嵌"

### B.2 变更清单

| 文件 | 变更 |
|------|------|
| `src/server/services/window-use-backfill-service.ts`（新建） | 回填服务：LLM 为存量卡生成 windowUse（与抽卡服务同一格式约定）→ 更新 DB → 同步 Markdown front matter → 立即按新键重嵌该卡。分批限流（10/批），断点 = "还有没有缺 windowUse 的行"，失败卡留待下轮 |
| `src/server/workers/vector-worker.ts` | `rebuildAllVectors` 只重建已有 windowUse 的卡（防止旧键重嵌）；`updateVector` 参数更名为 embeddingKey |
| `src/server/schedulers/vector-scheduler.ts` | 每小时 tick = 回填一批 → 常规重建，回填失败不影响重建 |
| `src/server/services/memory-service.ts` | 新增 `countMissingWindowUse` / `getMemoriesMissingWindowUse`（迁移进度指标） |
| `src/lib/vector/route-stats.ts`（新建） | 路由分布统计：`route_stats(date, route, count)` 按天聚合；`recordRouteStat` 旁路打点，失败静默不挡检索 |
| `src/lib/vector/retriever.ts` | `searchDetailed` 打点记录分类器决策（单点覆盖 chat 与 /api/memory/search） |
| `src/app/api/stats/routes/route.ts`（新建） | `GET /api/stats/routes?days=7`：分布 + 占比；已登记 api-route-contracts |
| `src/components/audit/AuditPanel.tsx` | 总览页新增"检索路由分布"卡片（各路由次数 + 占比条） |
| `src/config/purpose.config.ts`（新建） | I-5 遗留：系统目的静态配置，控制面新增 purpose.md（已入 file-watcher 排除名单） |
| `src/eval/fixtures.ts` | 补齐上一阶段遗漏的 6 条 daily 记忆的 windowUse（typecheck 抓出） |

### B.3 测试与验收

- 新增 13 个单测：回填链路（DB/Markdown/向量三步一致）、降级跳过、非法输出跳过、单卡失败不中断；路由统计聚合/窗口/静默语义；重建过滤/断点/单卡重嵌
- 全量 63 文件 / 588 用例通过；`tsc --noEmit` 零错误
- **迁移执行方式**：启动应用后 VectorScheduler 自动推进（每小时 10 张，约 6 小时补齐 55 张并全部按新键重嵌）；无需手动触发，无重复配额消耗

### B.4 遗留说明

- 生产库迁移进度可通过 `countMissingWindowUse` 观察（后续可加到健康度卡片，当前非必需）
- 上一阶段附录 A.1 写的"24 条记忆补 windowUse"实为 18 条 + 本阶段补的 6 条，特此更正
